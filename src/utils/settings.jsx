import { IoPersonOutline, IoLogOutOutline } from "react-icons/io5";

const settings = [
  { text: "Account", path: "account", icon: <IoPersonOutline /> },
  { text: "Logout", path: "logout", icon: <IoLogOutOutline /> },
];

export default settings;
